#include "protocol_lib.h"
#include "utility.h"

//-----------------------------------------------------------------------------
//---
//-----------------------------------------------------------------------------
int Read_XML(rt_Protocol *p, QString fn, bool only_config)
{
    p->Clear_Protocol();

    qDebug() << "file name: " << fn;
}
